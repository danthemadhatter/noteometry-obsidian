# Changelog

## 1.6.0 — 2026-04-17

- **Mobile:** right panel tabs (Input / Chat) + hide button; math palette anchors top on mobile instead of bottom; sidebar sections stack vertically so full names are visible; context menu scrolls when it overflows.
- **Chat:** paste (⌘V) and drag-and-drop images directly into chat instead of getting a `file://` link; clearer HTTP error bodies; drop ghost empty-content history entries that caused 400s after an image-only turn.
- **Canvas:** drop-ins raise to top on any pointer interaction; Clear Canvas now wipes drop-ins too.
- **Stamps:** Small / Normal sizes via right-click (was S/M/L floating toggle).
- **Build:** deploy only to `plugins/noteometry`; was also writing to `noteometry-build`.

## 1.5.0 — earlier

- Full source recovery, 14 drop-ins, PDF fix, Circuit Sniper.

